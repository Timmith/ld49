import { Color } from "three";

export const COLOR_BLACK: Readonly<Color> = new Color(0x000000);
export const COLOR_WHITE: Readonly<Color> = new Color(0xffffff);
export const COLOR_HOURGLASS_AVAILABLE: Readonly<Color> = new Color(0, 1, 0);
export const COLOR_HOURGLASS_UNAVAILABLE: Readonly<Color> = new Color(0.7, 0.7, 0.7);
